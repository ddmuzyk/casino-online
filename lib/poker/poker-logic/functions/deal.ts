// import { PlayerObject } from "@/pages/game/poker"

// export const dealCards = (players: Array<PlayerObject>, deck: Array<string>) => {
//   const playersCopy = players.map((player) => { 
//     return {
//       ...player,
//       cards: [...player.cards],
//     }
//   })

//   for (let player of playersCopy) {
//     if (player.money > 0) player.cards = [newDeck.pop() as string, newDeck.pop() as string];
//  }

//   return newPlayers;
// }

